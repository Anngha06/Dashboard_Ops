import Guard from '@/components/Guard';import Pills from '@/components/Pills';import { useAuth } from '@/lib/auth';export default function Pipelines(){const session=useAuth(s=>s.session)!;const canEdit=session.role!=='Viewer'&&session.role!=='InternChecker';return(<Guard area='Pipelines'><div className='max-w-6xl mx-auto p-6'><Pills tab='pipelines' canEdit={canEdit}/></div></Guard>)}