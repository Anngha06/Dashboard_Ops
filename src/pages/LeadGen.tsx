import Guard from '@/components/Guard';import SubTabs from '@/components/SubTabs';import Pills from '@/components/Pills';import { useAuth } from '@/lib/auth';import { useState } from 'react';const tabs=['Tech Society','Student Community','EdTech Websites'];export default function LeadGen(){const[t,setT]=useState(tabs[0]);const session=useAuth(s=>s.session)!;const canEdit=['Admin','LeadGenOwner'].includes(session.role);return(<Guard area='LeadGeneration'><div className='max-w-6xl mx-auto p-6'><SubTabs items={tabs} value={t} onChange={setT}/><Pills tab={t} canEdit={canEdit}/></div></Guard>) }