import Guard from '@/components/Guard';import Pills from '@/components/Pills';import { useAuth } from '@/lib/auth';export default function ColdCalling(){const session=useAuth(s=>s.session)!;const canEdit=['Admin','ColdCallingOwner'].includes(session.role);return(<Guard area='ColdCalling'><div className='max-w-6xl mx-auto p-6'><Pills tab='Cold Calling' canEdit={canEdit}/></div></Guard>) }